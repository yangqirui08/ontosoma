{
  "name": "@ontosoma/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "OntoSoma derivation engine: derive an ontology from code, serialize a diffable lockfile, gate on drift.",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "cli": "tsx src/cli.ts"
  },
  "dependencies": {
    "typescript": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "vitest": "^2.1.0"
  }
}
